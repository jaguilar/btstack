dependencies:
  # This dependency brings in the root of the btstack repo,
  # which we depend on in our CMakeLists.txt.
  bluekitchen/btstack_lib:
    git: https://github.com/jaguilar/btstack.git
    version: bluepad32
    type: git
